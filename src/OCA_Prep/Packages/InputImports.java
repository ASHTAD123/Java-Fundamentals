package OCA_Prep.Packages;

import java.nio.*; //a wildcard only matches classnames not files or packages
import java.nio.*.*; // you can only have 1 wildcard and it must be at end
import java.nio.files.Paths.* ;// can only import classes not methods 

public class InputImports {
    
}
